.mcp.json
tstdin

# Google OAuth credentials — never commit these
.scripts/gdrive_upload/token.json
.scripts/gdrive_upload/client_secret*.json
**/client_secret*.json

# Python bytecode cache
__pycache__/
*.pyc

# Lesson build artifacts — regenerable from <lesson-slug>/*.md + *-ficha.json
# (excludes presenter/'s own source code, which happens to live under a "slides" dir too)
**/slides/
!presenter/src/components/slides/
**/img/
**/fill.js
*.pptx

# Downloaded binaries — see poll-quicktest/bin/README.md to fetch again
poll-quicktest/bin/
